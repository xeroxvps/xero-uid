# Comma-separated browser origins allowed to call /api/*.
ALLOWED_ORIGINS=https://uids.xirxsms.xyz,https://xerox-uid.onrender.com,http://localhost:5173

# Optional Facebook session cookie. Keep the real value only in Render secrets.
FB_DEFAULT_COOKIE=

# Render injects PORT automatically. Local default is 5002.
PORT=5002
